# AUTH_PLAN.md

## 목표

- 새 DB를 만들 때 기본 계정은 `상운 / 960725`, `GUEST1 / 000000`, `GUEST2 / 000000` 3개만 생성한다.
- 앱 화면에서 임의 닉네임과 6자리 코드로 새 계정을 만들 수 없게 한다.
- 모든 학습 데이터는 로그인한 사용자의 `user_id`를 기준으로만 조회, 생성, 수정, 삭제한다.
- 테스트용 학습 데이터는 초기 DB에 넣지 않는다.
- HTTPS 배포와 앱 로그인은 분리해서 본다. HTTPS는 전송 보호이고, 앱 로그인은 사용자 데이터 분리용이다.

## 계정 정책

| 닉네임 | 코드 | 용도 |
| --- | --- | --- |
| 상운 | 960725 | 기본 개인 계정 |
| GUEST1 | 000000 | 게스트 계정 1 |
| GUEST2 | 000000 | 게스트 계정 2 |

- 위 목록에 없는 닉네임은 `/api/auth`에서 거부한다.
- 위 목록의 코드가 틀리면 `/api/auth`에서 거부한다.
- 서버 시작 시 기본 3계정은 항상 보정한다.
- 기존 DB에 다른 사용자가 남아 있어도 로그인과 API 인증에서는 허용하지 않는다.

## 데이터 분리 체크리스트

- `collections.user_id`는 학습 데이터 소유자를 결정하는 기준이다.
- 대그룹, 소그룹, 카드 조회는 항상 `collections.user_id = 현재 사용자 id`를 통과해야 한다.
- 카드 생성, 수정, 삭제는 현재 사용자의 소그룹에 속한 카드만 허용해야 한다.
- 회독 기록 조회는 현재 사용자의 소그룹과 연결된 기록만 반환해야 한다.
- 회독 완료 저장은 현재 사용자의 카드와 소그룹만 포함해야 한다.
- 묶음 연습은 기록 없는 연습으로 유지하고 공식 통계에 저장하지 않는다.
- 백업 내보내기와 복원은 현재 사용자 데이터만 대상으로 해야 한다.
- 설정값은 `users.id = 현재 사용자 id` 기준으로만 저장해야 한다.

## 초기화 체크리스트

- `docker compose down -v` 뒤 `docker compose up -d --build`를 실행하면 사용자는 3명이어야 한다.
- 새 DB의 `collections`, `groups`, `cards`, `study_rounds`, `reviews`는 비어 있어야 한다.
- `일본어 시험 표현`, `영어 단어 꼬꼬회독` 같은 샘플 학습 데이터가 없어야 한다.
- 기존 구버전 DB를 마이그레이션할 때 사용자 범위가 없던 학습 데이터는 `상운` 계정에 귀속한다.

## 수동 확인

1. `상운 / 960725` 로그인 성공
2. `GUEST1 / 000000` 로그인 성공
3. `GUEST2 / 000000` 로그인 성공
4. 임의 닉네임과 6자리 코드 로그인 실패
5. `상운`에서 만든 대그룹이 `GUEST1` 홈에 보이지 않음
6. `GUEST1`에서 만든 대그룹이 `상운` 홈에 보이지 않음
7. 각 계정의 백업 파일에 자기 데이터만 포함됨
8. PWA를 다시 홈 화면에 추가했을 때 새 꼬꼬 아이콘이 표시됨

## 남은 보안 결정

- `APP_USER` / `APP_PASSWORD` 기본 인증은 PWA 사용성 때문에 끌 수 있다.
- 공개 도메인에서 3계정만으로 쓰는 것이 불안하면 Cloudflare Access를 앞단에 붙인다.
- 게스트 계정 코드는 공유 코드이므로, 외부인이 도메인을 알면 게스트 데이터에 접근할 수 있다.
